package campoMinado;

import java.util.Scanner;

public class InteracaoComJogador {
	
	Scanner input = new Scanner(System.in);
	
	/*Jogo jogo = new Jogo();

	char acao;
	
	public void acoes() {
		System.out.println("Você quer:\nAbrir casa (A)\nColocar uma bandeira (B)");
		acao = input.next().charAt(0);
		if (acao == 'a' || acao == 'A') {
			jogo.casa();
		} else if (acao == 'b' || acao == 'B') {
			jogo.bandeira();
		} else {
			acoes();
		}
		input.close();
	}

	public void receberCoordenadas() {
		System.out.print("Insira sua linha: ");
		jogo.w = input.nextInt();
		jogo.x = (-(jogo.w - 9));

		System.out.print("Insira sua coluna: ");
		jogo.y = input.nextInt();

		if (jogo.x > 8 || jogo.y > 8 || jogo.x < 1 || jogo.y < 1) {
			receberCoordenadas();
		}
		input.close();
	}*/
	
	
	//teste
}
